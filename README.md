# micro-developer
An autonomous, zero-storage multi-agent Python developer loop built with CrewAI and OpenRouter (GLM-4.5-Air). Features a self-correcting feedback loop where a Programmer agent writes code and a QA Tester agent runs it locally via a custom subprocess sandbox, catching stack traces and fixing errors without human intervention.
